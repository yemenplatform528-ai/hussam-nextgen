# External Evidence Envelope Templates

These templates are deliberately placeholders. They are not production evidence and must not be promoted to CLOSED.

For each gate, replace every placeholder with facts from the real certification run, calculate the artifact SHA-256, and validate the directory with `scripts/production_evidence_protocol.py`.
